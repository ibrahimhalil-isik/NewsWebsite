@using Shared.Dtos
@model List<SlideDto>

<div class="container">
	<div class="page-title">
		<h3>Slayt İşlemleri</h3>
	</div>
	<div class="row">

		<div class="col-md-12 col-lg-12">
			<div class="card">
				<div class="card-header">
					<div class="row">
						<div class="col-9" style="line-height:50px;">Slaytlar</div>
						<div class="col-3"><a href="/Slide/Add" style="float:right;color: white;background-color:green;" class="btn float-right m-2"><i class="fa fa-plus"></i></a></div>
					</div>
				</div>
				<div class="card-body">
					<p class="card-title">Slaytlar ile ilgili tüm işlemleri buradan yapabilirsiniz. Toplam Aktif Veri Sayısı : @Model.Where(x => x.SlideStatus == true).Count()</p>
					<table class="table table-hover" id="dataTables-example" width="100%">
						<thead>
							<tr>
								<th>Resim</th>
								<th>Başlık</th>
								<th>İçerik</th>
								<th>Haber</th>
								<th>Durum</th>
								<th style="min-width:110px;">İşlemler</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var item in Model)
							{
								<tr>
									@* <td><img style=" max-width: 150px; " src="@Url.Content(@conf.GetSection("MySettings")["ApiUrl"] + item.Image)" /></td> *@
									<td><img style=" max-width: 150px; " src="@Url.Content("https://localhost:7041" + item.Image)" /></td>
									<td>@item.Title</td>
									<td>@item.Content</td>
									<td>@item.News</td>
									<td>
										@if (item.SlideStatus)
										{
											<span class="badge bg-info">Aktif</span>
										}
										else
										{
											<span class="badge bg-danger">Pasif</span>
										}
									</td>
									<td>
										<a href="/Slide/Update?id=@item.SlideId" class="btn btn-primary mb-2"><i class="fa fa-edit"></i></a>
										<a href="/Slide/Delete?id=@item.SlideId" class="btn btn-danger mb-2" style="width: 44px;"><i class="fa fa-times"></i></a>
									</td>
								</tr>
							}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
</div>